# WSL 1Panel API Fixtures

本文已 superseded，active 真源改为 [1Panel CLI Validation Workflow](./onepanel-cli-validation-workflow.md)。

保留此文件仅为兼容旧引用；WSL fixture 的环境路径、对象集合、`fixture plan/apply/cleanup` 与 `suite run` / `ledger refresh` 闭环统一看 active runbook。
