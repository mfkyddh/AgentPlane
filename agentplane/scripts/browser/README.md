# Browser Helpers

Optional local helper scripts for browser/CDP workflows belong in this directory.

The OpenClaw browser-bridge skill refers to helper names under `agentplane/scripts/browser/` when you choose to keep those utilities in the repository.
