# Internal Remote Helpers

`agentplane/scripts/internal/remote/` is the handoff directory for internal remote helper scripts.

- Keep informal examples and helper-only scripts here.
- Public compatibility entrypoints stay under `agentplane/scripts/remote/`.
- Some internal helpers may still be reused by formal CLI flows; that reuse does not make them public entrypoints.
