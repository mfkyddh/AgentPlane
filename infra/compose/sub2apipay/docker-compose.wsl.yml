services:
  sub2apipay:
    image: ${SUB2APIPAY_IMAGE_REF:-sub2apipay-dev:local}
    container_name: sub2apipay-dev
    restart: unless-stopped

    env_file:
      - /root/work/AgentPlane/secrets/services/sub2apipay.wsl.env

    ports:
      - "0.0.0.0:18091:3000"

    volumes:
      - /data/sub2apipay/data:/data

    healthcheck:
      test: ["CMD-SHELL", "wget -q -O /dev/null http://127.0.0.1:3000/pay"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

    networks:
      - zqf_network

networks:
  zqf_network:
    external: true
