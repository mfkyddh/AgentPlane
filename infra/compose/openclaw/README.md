# OpenClaw

- Tracked templates live here: `docker-compose.wsl.yml` and `docker-compose.prod0.yml`.
- The install helper targets the WSL template by default.
- Real token and provider keys live in `secrets/services/openclaw.env`.
- The tracked template is `templates/services/openclaw.env.example`.
