# Legacy transitional projection-only template.
# Canonical source moved to templates/services/postgres/admin.env.example.
# Keep this file only for migration context that still references postgres.env.
POSTGRES_DB=postgres
POSTGRES_USER=replace-with-postgres-user
POSTGRES_PASSWORD=replace-with-postgres-password
